Введення/виведення можна робити:
1) через файли;
2) через STDIN;
3) можна поєднувати читання з файла та виведення на консоль через printf, як на С та С++.

Приклад вхфдного файлу
53 49 50
35 58 62
читається в масив $InputData, як вектор рядків (діапазон індексів масиву: від 0 до 5).

Для деяких задач, наприклад, Дележ яблок - 1, ділення націло потрібно реалізуватти через приведення
типів до цілого явним способом. В інших задачах, наприклад, Число десятків, ділення націло виконується
стандартним способом для цілих чисел через /.

Виведення можна виконувати через printf з форматуванням рядка, а можна через змінну типу string,
в якій через "." об'єднати дані текстового та числових типів, як в задачі Следующее и предыдущее.

xDebuger налаштувати, як показано в https://www.youtube.com/watch?v=ZWuRCLb5aIc&t=332s
Налаштування вкладок для Debug в File->Settings->language...->PHP->... непотрібне. Перед використанням
ввімкнути вгорі на панелі значок "слухавки", а потім вже "дебажити".
Для дебагінга php-сценарія дані потрібно прочитати обов'язково з файла: під час читання з STDIN
в режимі debug код не виконується (можливо треба  ввести з консолі, а потім якось переключитись на
вікно Debug - питання відкрите).

Формула для обчислення остачі на інтервалі (-y..+y): (x%y+y)%y

В PHP оператори логічних відношень пишуть словами (як на Паскалі), прості логічні умови мають
два види "не дорівнює" та "тотожна рівність". Якщо напистаи словами оператори логічних відношень,
тоді їх пріорітет самий низький http://php.net/manual/ru/language.operators.precedence.php

Документиція:
1) форматування sprintf   http://php.net/manual/ru/function.sprintf.php
2) по функціях (математичних) http://php.net/manual/ru/book.math.php
3) логічні оператори
    http://php.net/manual/ru/language.operators.logical.php
    http://www.php.su/lessons/?lesson_1
4) оператори порівнянь  http://php.net/manual/ru/language.operators.comparison.php
5) конвертація типів  http://php.net/manual/ru/language.types.type-juggling.php#language.types.typecasting
6) останов скрипта  http://php.net/manual/ru/function.return.php
7) область видимості змінних
    http://php.net/manual/ru/language.variables.scope.php
    http://on-line-teaching.com/php/struct.vars.htm
8) switch та альтернативний синтаксис для HTML http://php.net/manual/ru/control-structures.switch.php
9) підключити інші файли (класи) до скрипту http://on-line-teaching.com/php/struct.structs.htm
10)




